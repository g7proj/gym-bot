# Frontend (React)

This directory will contain the React SPA for user login and preference management.

## Setup

1. Install Node.js and npm.
2. Run `npx create-react-app .` in this directory.
3. Implement components for login form and preferences editor.
4. Connect to the FastAPI backend at `http://localhost:8000`.